{
  "name": "arsalan-portfolio-pdf-workspace",
  "version": "2.1.0",
  "private": true,
  "description": "Development checks for the static portfolio PDF workspace",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:ui": "node --test tests/pdf-workspace.ui.mjs",
    "test:browser": "node tests/pdf-workspace.browser.cjs"
  },
  "devDependencies": {
    "happy-dom": "20.14.5",
    "playwright": "1.62.1"
  }
}
